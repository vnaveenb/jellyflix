# JellyFlix Configuration
# Leave empty to automatically use the same-origin reverse proxy (/jellyfin-api)
# Set a custom URL only if developing locally with a standalone remote Jellyfin server
VITE_JELLYFIN_URL=
